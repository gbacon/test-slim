{
  "name": "test-slim",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y cpanminus build-essential && cpanm --local-lib=$HOME/perl5 --notest --installdeps .",
}
